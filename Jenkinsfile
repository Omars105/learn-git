pipeline {
    agent any 

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        } // Closes Stage
        
        stage('Test') {
            steps {
                echo 'Testing...'
            }
        } // Closes Stage
    } // Closes ALL Stages
} // Closes the Pipeline (This is likely the one you are missing!)
