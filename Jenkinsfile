pipeline {

  agent any

  stages {

          stage("build") {

             steps {
               exho 'building the app'
             }
          }
    stage("test") {
      
             steps {
               echo 'testing the app'
             }
    }
  }
